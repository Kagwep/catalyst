# Secrets — never commit (repo is public)
.env
.env.*
!.env.example

# Supabase CLI local state (ephemeral; not secret but not worth committing)
supabase/.temp/
supabase/.branches/

# Python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/

# Flows HTTP cache (last-good ETF-flow pages)
.cache/

# SQLite database + WAL/shm sidecar files
*.db
*.db-wal
*.db-shm
out.json
